const autentificado = true;

if(autentificado){
    console.log ('Si puedes ver netflix')
} else {
    console.log('No puedes acceder')
}

// Operador ternario
console.log (autentificado ? 'Si está autentificado' : 'No está autentificado');